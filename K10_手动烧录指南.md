# K10 手动烧录 51_mic_wifi 固件指南

## ⚠️ 当前问题
K10 LCD 仍然显示 GIF 图片（50_codebuddy 固件），说明 51_mic_wifi 固件未成功烧录。

## ✅ 解决方案：使用 IDE 手动编译烧录

---

## 方法 1：使用 VS Code + PlatformIO（推荐）

### 步骤 1：打开项目
1. 启动 **VS Code**
2. 点击 **文件 → 打开文件夹**
3. 选择：`C:\Users\4090\Desktop\dfk10_arduino_demo-master`
4. 等待 PlatformIO 加载完成（底部状态栏显示项目名称）

### 步骤 2：确认配置
1. 打开 `platformio.ini` 文件
2. 确认 `default_envs = 51_mic_wifi`（应该已经是）

### 步骤 3：清理并重新编译
1. 点击底部状态栏的 **垃圾桶图标**（Clean）
2. 等待清理完成
3. 点击底部状态栏的 **✓ 图标**（Build）
4. 等待编译完成（首次编译可能需要 2-5 分钟）

### 步骤 4：烧录到 K10
1. **K10 进入下载模式**：
   - 按住 **BOOT** 按钮
   - 短按 **RESET** 按钮
   - 松开 BOOT 按钮
   - 确认 COM4 端口出现在设备管理器中

2. 点击底部状态栏的 **→ 图标**（Upload）
3. 等待烧录完成
4. 烧录成功后，按 K10 的 **RESET** 按钮

### 步骤 5：验证
观察 K10 的 LCD 屏幕，应该显示：
```
=== CodeBuddy P1 ===
ESP-NOW: ✓ 已配对
Dongle: e0:72:a1:xx
信道: 1

⏸️ 待命
按键 A 开始发流
```

---

## 方法 2：使用 Arduino IDE

### 步骤 1：打开固件文件
1. 启动 **Arduino IDE**
2. 打开文件：`C:\Users\4090\Desktop\dfk10_arduino_demo-master\examples\51_mic_wifi\51_mic_wifi.ino`

### 步骤 2：配置开发板
**工具 → 开发板 → ESP32 Arduino → ESP32S3 Dev Module**

**配置参数**：
- **USB CDC On Boot**: **Enabled**
- **Flash Size**: **16MB (128Mb)**
- **Partition Scheme**: **16MB Flash (3MB APP/9.9MB FATFS)**
- **PSRAM**: **OPI PSRAM**
- **Upload Speed**: **921600**

**工具 → 端口 → COM4**

### 步骤 3：处理依赖库
如果编译报错缺少库，安装以下库：
- **工具 → 管理库** → 搜索并安装：
  - `LovyanGFX`（如果使用 Lcd_handler.hpp）
  - `ESP32` 核心库（应该已安装）

### 步骤 4：编译
1. 点击 **验证** 按钮（✓）
2. 等待编译完成
3. 检查编译输出，确认无错误

### 步骤 5：烧录
1. **K10 进入下载模式**：
   - 按住 **BOOT** 按钮
   - 短按 **RESET** 按钮
   - 松开 BOOT 按钮

2. 点击 **上传** 按钮（→）
3. 等待烧录完成

### 步骤 6：验证
烧录完成后按 K10 的 **RESET** 按钮，观察 LCD 显示。

---

## 方法 3：如果 IDE 不可用，使用命令行工具

### 检查 Python 和 esptool
```powershell
python --version
python -m pip install esptool
```

### 手动编译（如果有预编译固件）
如果之前的编译产生了正确的固件，可以直接烧录：

```powershell
cd C:\Users\4090\Desktop\dfk10_arduino_demo-master\.pio\build\51_mic_wifi

# 使用 ESP-IDF 环境
. C:\Users\4090\esp\esp-idf\export.ps1

# 烧录
python -m esptool --chip esp32s3 --port COM4 --baud 921600 \
  --before default_reset --after hard_reset write_flash -z \
  --flash_mode dio --flash_freq 80m --flash_size 16MB \
  0x0 bootloader.bin 0x8000 partitions.bin 0x10000 firmware.bin
```

**问题**：如果该目录的 firmware.bin 是旧的 50_codebuddy，这不会有效。

---

## 🔍 如何确认固件正确

### 检查源代码目录
确认 K10 固件源文件是否正确：
```
C:\Users\4090\Desktop\dfk10_arduino_demo-master\examples\51_mic_wifi\51_mic_wifi.ino
```

文件开头应该是：
```cpp
/**
 * @file 51_mic_espnow.ino
 * @brief CodeBuddy P1: I2S 麦克风采集 + ESP-NOW 音频发送
 */
```

### 检查 platformio.ini
```
default_envs = 51_mic_wifi
```

### 检查 src_dir
```
src_dir = examples/${platformio.default_envs}
```

这意味着编译时会读取 `examples/51_mic_wifi/` 目录。

---

## 🐛 常见问题

### 问题 1：编译找不到库文件
**报错**：`Lcd_handler.hpp: No such file or directory`

**解决**：
- 确认 `src/` 目录下有所需的头文件
- 或修改 `#include "Lcd_handler.hpp"` 为完整路径

### 问题 2：烧录后仍显示 GIF
**原因**：
- 固件未成功写入
- Flash 分区表冲突
- 固件二进制文件是旧版本

**解决**：
1. 清理所有编译缓存
2. 重新编译生成新的 firmware.bin
3. 确认烧录过程无错误
4. 烧录后按 RESET 按钮

### 问题 3：LCD 黑屏
**可能原因**：
- 固件启动失败
- I2C 初始化失败
- LCD 驱动问题

**排查**：
- 查看串口日志（COM4，115200 波特率）
- 检查是否有启动日志输出

---

## 📝 下一步行动

**立即尝试**：
1. **打开 VS Code + PlatformIO**
2. **打开项目文件夹**
3. **Clean → Build → Upload**
4. **观察 K10 LCD 变化**

或者：
1. **打开 Arduino IDE**
2. **打开 51_mic_wifi.ino**
3. **配置开发板 → 编译 → 上传**

**完成后告诉我结果！**

---

## 💡 临时验证方案

如果仍然无法烧录，可以：
1. 暂时使用 Dongle 串口日志验证通信
2. 手动触发 K10 的 ESP-NOW 发送（如果有按键可用）
3. 或者先验证 Dongle 的 USB Audio 枚举是否正常

但最根本的解决方案是成功烧录 51_mic_wifi 固件到 K10。
