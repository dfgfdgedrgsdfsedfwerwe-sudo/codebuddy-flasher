# K10 (COM4) 烧录指南

## 📋 配置确认

✅ **Dongle MAC 地址已配置**：`e0:72:a1:d4:8f:e0` (COM6)  
✅ **WiFi 信道**：1（匹配 Dongle）  
✅ **固件路径**：`examples/51_mic_wifi/51_mic_espnow.ino`  
✅ **platformio.ini**：已设置 `default_envs = 51_mic_wifi`

---

## 方法一：使用 PlatformIO（推荐）

### 1. 打开 PlatformIO IDE
- 启动 VS Code
- 打开项目文件夹：`C:\Users\4090\Desktop\dfk10_arduino_demo-master`

### 2. 编译和烧录
点击底部状态栏的按钮：
- **Build** (✓) - 编译固件
- **Upload** (→) - 上传到 COM4

或者使用终端命令：
```bash
cd C:\Users\4090\Desktop\dfk10_arduino_demo-master
pio run -e 51_mic_wifi -t upload --upload-port COM4
```

---

## 方法二：使用 Arduino IDE

### 1. 打开固件
- 启动 Arduino IDE
- 打开文件：`examples/51_mic_wifi/51_mic_espnow.ino`

### 2. 配置开发板
**工具 → 开发板 → ESP32 Arduino → ESP32S3 Dev Module**

**工具 → 端口 → COM4**

**开发板参数**：
- **USB CDC On Boot**: Enabled
- **Flash Size**: 16MB (128Mb)
- **Partition Scheme**: 16MB Flash (3MB APP/9.9MB FATFS)
- **PSRAM**: OPI PSRAM

### 3. 编译上传
点击 **上传** 按钮 (→)

---

## 方法三：使用预编译固件（如果已编译）

### 1. 查找固件二进制文件
```
.pio/build/51_mic_wifi/firmware.bin
```

### 2. 使用 esptool.py 烧录
```bash
python -m esptool --chip esp32s3 --port COM4 --baud 921600 ^
  --before default_reset --after hard_reset write_flash ^
  -z --flash_mode dio --flash_freq 80m --flash_size 16MB ^
  0x0 bootloader.bin ^
  0x8000 partitions.bin ^
  0x10000 firmware.bin
```

---

## 烧录后验证

### 1. 查看串口日志
```bash
# PlatformIO
pio device monitor -p COM4 -b 115200

# 或 Arduino IDE
工具 → 串口监视器 → 115200 波特率
```

### 2. 检查关键日志
应该看到：
```
=== CodeBuddy P1: I2S Mic + ESP-NOW ===
Chip: esp32s3
MAC: 3c:dc:75:6d:7f:b4
WiFi channel: 1

=== I2C Scan on SDA=47 SCL=48 ===
  Found device at 0x11  (ES7243E ADC)
  Found device at 0x20  (XL95x5 IO Expander)
  Found device at 0x38  (AHT20 Temp/Humidity)
Total: 3 device(s)

Initializing ES7243E at 0x11...
ES7243E initialized successfully

ESP-NOW initialized
Peer added: e0:72:a1:d4:8f:e0 (channel 1)

=== LCD Initialized ===
Press Button A to start/stop streaming
```

### 3. 按键测试
- **按下按键 A**（左下角）：开始发流
  - LCD 显示：`📡 发流中`
  - LED 闪烁：Dongle (COM6) 的 LED 应快闪
  - 串口日志：`Sent packet #1, #2, #3...`

- **再次按下按键 A**：停止发流
  - LCD 显示：`⏸️ 已停止`

---

## 故障排除

### 烧录失败
- **"串口打不开"**：确认 COM4 未被其他程序占用
- **"无法连接"**：按住 BOOT 键，点击 RESET 键，进入下载模式
- **"芯片不响应"**：检查 USB 线是否连接到 **COM 口**（不是 USB 口）

### 日志无输出
- 检查波特率：**115200**
- 检查串口选择：**COM4**
- 重新上电/按 RESET 键

### I2C 扫描失败
- 如果看到 `Total: 0 device(s)`，说明 I2C 总线未响应
- 确认引脚定义正确：`SDA=47, SCL=48`
- 检查硬件连接

### ES7243E 初始化失败
- 如果未找到 `0x11` 设备，说明麦克风 ADC 未响应
- 尝试翻转 `MIC_CHANNEL_OFFSET`（0→1 或 1→0）
- 检查 I2S 引脚配置

### ESP-NOW 配对失败
- 确认 Dongle (COM6) 已上电并正常运行
- 检查 Dongle MAC 地址：`e0:72:a1:d4:8f:e0`
- 确认两端 WiFi 信道一致（都是 1）

### 按键无响应
- 检查按键 A 的 GPIO 定义（通过 IO 扩展器 XL95x5）
- 查看串口是否有 `Button A pressed/released` 日志
- 确认 `init_board()` 成功初始化

---

## 联调测试步骤

### 1. 准备两台设备
- **COM6 Dongle**：USB 线插入 **USB 口**（枚举为 USB Audio）
- **COM4 K10**：USB 线插入 **COM 口**（查看串口日志）

### 2. 启动测试
1. 给 COM6 Dongle 上电
   - LED 应显示**心跳模式**（双闪）
   - Windows 设备管理器应显示 `CodeBuddy Audio` 和 `CodeBuddy Microphone`

2. 给 COM4 K10 上电
   - LCD 显示 ESP-NOW 状态
   - 串口日志显示 `Peer added: e0:72:a1:d4:8f:e0`

3. 按下 K10 的**按键 A**
   - K10 LCD 显示：`📡 发流中`
   - K10 串口日志：`Sent packet #1, #2, #3...`
   - Dongle LED：快速闪烁（表示接收数据）
   - Dongle 串口日志（COM6 monitor）：`ESP-NOW audio packet received`

4. 对着 K10 麦克风说话
   - 打开 Windows **声音设置 → 输入 → CodeBuddy Microphone**
   - 应该能看到音量条跳动
   - 使用录音软件（Audacity）录制测试

### 3. 预期结果
✅ K10 麦克风捕获音频  
✅ ESP-NOW 无线传输到 Dongle  
✅ Dongle 转发到 PC（USB Audio）  
✅ PC 录音软件捕获音频  
✅ LED 指示数据传输状态

---

## 下一步

✅ K10 固件配置完成  
⏳ **等待烧录到 COM4**  
⏳ 联调测试 K10 ↔ Dongle ↔ PC  

**烧录完成后，按照上述验证步骤测试！**
